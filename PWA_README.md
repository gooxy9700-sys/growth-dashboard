# 与己书 PWA

个人本地优先的考研成长自律看板 PWA。公开部署版本不包含私人规划或健康资料。

## 已实现

- 今日三件重点任务和任务状态。
- 首页总计划完成率、今日进度、待完成分钟、本周专注和按类别进度。
- 今日时间线：显示开始时间、工具、方法和预期产出。
- 秋季周循环计划：以 2026-08-31 为第 1 教学周，自动按周一至周日加载模板；2026-08-30 前显示预览模式。
- 桌面看板首页：7 天日期导航、07:00-23:30 当日时间轴、刚性课程/弹性事项区分、周数学/英语/运动进度和备注复盘。
- 弹性事项支持拖动、双击编辑、删除和勾选；事项修改与备注进入同一状态快照，可随 Supabase 同步。
- 计划日期可切换，支持标准周、实验周、考试周、全休日模板；模板任务可手动新增、编辑、删除。
- 任务创建、类别、日期、预计时间、优先级与筛选。
- 任务可记录开始时间、工具、方法和预期产出。
- 番茄钟支持数学 45+10、英语 30+5、通用 25+5 模板，自定义时长、暂停、结束本次、重置和任务锁定；结束本次会记录实际分钟。
- 支持白噪音/雨声、音量、浏览器提醒和屏幕常亮锁（浏览器支持时启用）。
- 校园跑显示本周剩余可计入次数；健身记录覆盖推/拉/腿/核心、训练分钟、组数、次数和重量。
- 体重每日快速录入，最近记录与最新体重会进入统计页。
- 统计页按日/周/月展示学习柱状图、跑步/健身趋势、体重趋势和按实际专注分钟计算的各科学习占比饼图；ECharts 可用时优先使用，离线或 CDN 失败时回退到 Canvas。
- 月度目标默认比较学习 1200 分钟、跑步 12 km、健身 8 次，并显示已达标、按计划或需追回状态。
- localStorage 本地存储，JSON 导出和导入。
- 计划导入：一句话、多行文本、TXT/Markdown、CSV/TSV、XLSX、PDF、DOCX；图片可尝试 OCR。所有识别结果先预览，确认后才写入。
- Manifest 与 Service Worker，支持安装和离线打开已有内容。

## 启动

```powershell
& 'C:\Users\Dovoer\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe' -m http.server 4173
```

在本目录运行后，浏览器访问 `http://127.0.0.1:4173/`。

## 首次使用

1. 保留或替换系统给出的三件种子任务。
2. 每天只排最多三件“重点”任务；其余任务设为正常或可选。
3. 完成学习、跑步或训练后，在对应页面记录结果；数据统计页按日、周、月查看趋势。
4. 每周至少导出一次 JSON 到你自己可信任的位置。v1 不做账号和云同步。

## 导入规则

- 文字：一行一项任务；可写“周二 14:00 做高数 90 分钟，手机看网课，平板刷题，最后用 AI 复盘”。
- CSV/TSV/XLSX：推荐列名为 `日期、星期、时间、任务、类别、分钟、工具、方法、产出、优先级、状态`。XLSX 会扫描前 20 行，自动寻找含“任务/Task/Title/名称”的工作表。
- PDF/DOCX：浏览器先提取正文，再按行生成预览任务；扫描图片型 PDF 可能没有文字，需要切到图片 OCR 或粘贴文字。
- 图片 OCR：首次需要联网加载 OCR 引擎；如果引擎加载或识别失败，界面会明确提示，不会伪造任务。
- 导入默认采用当前日期；识别到“周一/周二”等会映射到从当前日期起最近的对应星期。确认前可以取消，不会污染本地计划。

## 验证记录

- 已通过 bundled Node 语法检查：`app.js`、`service-worker.js`。
- 已在本地服务器检查桌面首屏、今日任务、番茄专注、校园跑、健身记录、数据统计、任务弹窗和导入弹窗，浏览器控制台 error/warn 为 0。
- Service Worker 使用 `yijishu-v13`，导航与同源静态资源网络优先、失败回退缓存；页面脚本入口为 `app.js?v=15`，同步脚本入口为 `sync.js?v=2`。

## Supabase 配置边界

- `supabase-config.example.js` 提供字段示例；部署前编辑根目录的 `supabase-config.js` 并填写项目 URL 和 publishable/anon key。该 key 可随静态站点发布，安全边界由 RLS 提供。
- `supabase-schema.sql` 使用单行 JSON 快照表，并通过 RLS 约束为“登录用户只能读写自己的 `user_id`”。
- 当前代码默认只使用 localStorage，不会因为缺少配置而请求云端；真实登录与同步接入将在下一阶段完成。
- 静态站点中绝不能放置 `service_role` key；部署前只允许使用 publishable/anon key。
- 隐私声明位于 `privacy.html`，GitHub Pages 步骤位于 `DEPLOYMENT.md`。
- `sync.js` 已接入可选认证流程：配置有效时提供登录、注册、密码重置、退出和快照同步；配置为空时保持本地模式。
- 同步策略为本地快照最后写入时间优先，联网失败显示待联网状态，恢复联网后自动重试。
- 配置项目并执行 `supabase-schema.sql` 后，`sync.js` 会订阅同账号的 `growth_snapshots` 变更；其他设备写入较新快照时会自动载入。
- `health-kit-adapter.js` 是未来华为 Health Kit 的边界接口；当前只支持手动录入，不会读取或上传设备健康数据。

## 后续迭代

先连续使用 14 天。只根据真实使用问题增加功能，例如课表周次导入、任务估时偏差和真实导入样本验证；手机和平板适配放在桌面端稳定后。不先加入 AI 排程、广告、账号、社交或微信小程序迁移。
