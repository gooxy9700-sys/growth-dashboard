# 与己书看板设计系统

## 产品场景

这是一个高频使用的个人成长工具。首页的主要任务是让用户在几秒内确认今天的日期、下一段安排和完成状态，并能快速切换到另一日期。

## 视觉方向

- 模式：Operate
- 世界：纸质学习账本。冷白页面、墨绿秩序、朱砂色行动提示；课程与记录是内容本身，不用装饰抢走注意力。
- 主题：深墨绿正文、冷白工作区、苔绿完成/已选状态、朱砂色用于新增与开始专注。
- 密度：中等偏高；信息优先，但通过明确区块、留白与时间列建立扫描节奏。
- 形状：小圆角（6-8px），只用于可操作容器和状态。
- 阴影：默认无阴影；仅悬停的日程项用低对比软阴影表达可操作性。

## 色彩

- --ink: #202427，正文与标题
- --muted: #687276，辅助说明
- --ground: #eef1f0，应用底色
- --paper: #fbfcfb，内容表面
- --teal: #c6533d，唯一行动色，用于主按钮、选中态和进度
- --blue: #426d82，刚性课程标识

## 首页结构

1. 轻量全局栏：品牌、同步状态和数据操作
2. 左侧稳定导航（桌面）或底部拇指导航（手机）
3. 日期上下文和日期选择带，当前日期明确高亮
4. 本周进度和单日课程执行流
5. 备注、计划、进度、清单和时间线，保持三端可达

## 跨设备结构

- 手机（≤700px）：单列任务流与底部导航，所有今日模块可访问。
- 平板（701-1180px）：紧凑左侧导航；任务页为日程主列 + 记录/计划副列，横竖屏均不退回手机导航。
- 桌面（>1180px）：完整侧栏和宽阔分析区；统计图保持桌面密度。

## 交互原则

- 打开首页默认今天。
- 日期切换只刷新当天课程表和备注，不改变其他数据。
- 课程块点击区域保持稳定；勾选完成不改变布局尺寸。
- 刚性课程不可编辑，弹性事项保留双击编辑、拖动、删除。
- 手机端课程表允许内部横向浏览，但页面本身不产生横向溢出。
- 过渡只用于按压、完成态、可选日程悬停和短暂提示；高频记录操作不播放编排动画。

## 质量门槛

- 390px 手机宽度没有页面横向溢出。
- 键盘焦点可见，表单保持原生输入语义。
- 课程状态有未完成、完成、不可编辑三种清晰状态。
